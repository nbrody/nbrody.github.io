import BorelHarishChandra.AlgebraicGroups.LinearAlgebraicGroups
import BorelHarishChandra.Prerequisites.TopologyMeasure

/-!
# Real Points as Lie Groups

The theorem compares integral points `G(ℤ)` with the real Lie group `G(ℝ)`. This
module is for the topology and group structure on real points.
-/

namespace BorelHarishChandra.LieGroups.RealPoints

theorem placeholder : True := by
  trivial

end BorelHarishChandra.LieGroups.RealPoints
