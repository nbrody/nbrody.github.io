import BorelHarishChandra.AlgebraicGroups.RationalCharacters
import BorelHarishChandra.ArithmeticSubgroups.IntegralPoints
import BorelHarishChandra.Lattices.FiniteCovolume

/-!
# Borel-Harish-Chandra Statement

This module is reserved for the eventual formal statement of the theorem.

Informally: if `G` is a linear algebraic group over `ℚ` with no non-trivial
`ℚ`-characters, then the arithmetic subgroup `G(ℤ)` is a lattice in `G(ℝ)`.
-/

namespace BorelHarishChandra.MainTheorem.Statement

theorem placeholder : True := by
  trivial

end BorelHarishChandra.MainTheorem.Statement
