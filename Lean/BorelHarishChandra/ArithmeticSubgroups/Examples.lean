import BorelHarishChandra.ArithmeticSubgroups.IntegralPoints

/-!
# Concrete Examples

This module is for toy cases such as `SL(n, ℤ)` inside `SL(n, ℝ)`. These examples
are the best place to learn the relevant Mathlib APIs before attempting the general
statement.
-/

namespace BorelHarishChandra.ArithmeticSubgroups.Examples

theorem placeholder : True := by
  trivial

end BorelHarishChandra.ArithmeticSubgroups.Examples
